name: Upstream versions

on:
  merge_group:
  push:
    branches:
      - build
    paths:
      - '.github/workflows/*.yaml'
      - 'Makefile'
      - 'config/refs-*.json'
      - 'config/remotes.json'
      - 'config/upstream-versions.json'
      - 'scripts/check_upstream_versions.py'
      - 'scripts/ensure_act.sh'
      - 'scripts/run_github_actions_with_act.sh'
      - 'scripts/reconstruction_common.py'
      - 'scripts/test_check_upstream_versions.py'
      - 'scripts/test_support.py'
      - 'docs/scripts/docs-workflow.Dockerfile'
  pull_request:
    branches:
      - build
    paths:
      - '.github/workflows/*.yaml'
      - 'Makefile'
      - 'config/refs-*.json'
      - 'config/remotes.json'
      - 'config/upstream-versions.json'
      - 'scripts/check_upstream_versions.py'
      - 'scripts/ensure_act.sh'
      - 'scripts/run_github_actions_with_act.sh'
      - 'scripts/reconstruction_common.py'
      - 'scripts/test_check_upstream_versions.py'
      - 'scripts/test_support.py'
      - 'docs/scripts/docs-workflow.Dockerfile'
  schedule:
    - cron: '23 2 * * 0'
  workflow_dispatch:
    inputs:
      mode:
        default: policy
        description: Version-check failure mode.
        options:
          - advisory
          - policy
          - strict
        required: true
        type: choice
      only:
        default: ''
        description: Optional comma-separated version check IDs.
        required: false
        type: string

permissions:
  contents: read

concurrency:
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}
  group: >-
    ${{ github.workflow }}-${{
      github.event_name == 'pull_request' && github.event.pull_request.number || github.run_id
    }}

jobs:
  upstream-versions:
    name: Check upstream versions
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout build branch
        uses: actions/checkout@v7
        with:
          fetch-depth: 1

      - name: Fetch source refs required by the build branch
        shell: bash
        run: |
          set -euo pipefail
          git fetch --force --no-tags origin '+refs/heads/source/*:refs/heads/source/*'
          git fetch --force origin '+refs/tags/source/*:refs/tags/source/*'

      - name: Check upstream versions
        env:
          UPSTREAM_VERSION_FORMAT: github
          UPSTREAM_VERSION_MODE: ${{ inputs.mode || 'policy' }}
          UPSTREAM_VERSION_ONLY: ${{ inputs.only || '' }}
        run: make check-upstream-versions
